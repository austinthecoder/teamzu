%h2
  Cancel Account
  
= form_for resource, :as => resource_name, :url => registration_path(resource_name), :html => {:method => :delete, :id => 'delete_user'} do |f|
  
  %p Are you sure you want to cancel your account?
  
  %div
    = submit_button "Yes, cancel my account"
    = link_to 'No, go back', edit_user_registration_path
